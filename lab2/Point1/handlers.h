#ifndef HANDLERS_H
#define HANDLERS_H

int HandlerOptL(const char* inputStr);
int HandlerOptR(const char* inputStr);
int HandlerOptU(const char* inputStr);
int HandlerOptN(const char* inputStr);
int HandlerOptC(unsigned int seed, int argc, char* argv[]);

#endif
